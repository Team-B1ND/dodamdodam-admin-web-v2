import SectionHeader from "../Common/PageHeader/SectionHeader";

const Authority = () => {
  return (
    <SectionHeader
      title="서비스 권한"
      description="사용자들의 권한을 관리할 수 있습니다."
    />
  );
};

export default Authority;
